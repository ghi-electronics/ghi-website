---
title: "UartPort Class"
sidebar_label: "UartPort"
---

# UartPort Class

**Namespace:** `GHIElectronics.TinyCLR.Pins` · **Assembly:** `GHIElectronics.TinyCLR.Pins`

UART port definitions.

```csharp
public static class UartPort
```

## Fields

### Uart4

```csharp
public const string Uart4
```

UART port on PD0 (TX) and PD1 (RX), PB0 (CTS) and PA15 (RTS).

### Uart6

```csharp
public const string Uart6
```

UART port on PC6 (TX) and PC7 (RX).

### Uart7

```csharp
public const string Uart7
```

UART port on PE8 (TX) and PE7 (RX).

### Uart1

```csharp
public const string Uart1
```

UART port on PA9 (TX) and PA10 (RX).

### Uart3

```csharp
public const string Uart3
```

UART port on PD8 (TX) and PD9 (RX).

### Uart4

```csharp
public const string Uart4
```

UART port on PD0 (TX) and PD1 (RX), PB0 (CTS) and PA15 (RTS).

### Uart6

```csharp
public const string Uart6
```

UART port on PC6 (TX) and PC7 (RX).

### Uart7

```csharp
public const string Uart7
```

UART port on PE8 (TX) and PE7 (RX), PE10 (CTS) and PE9 (RTS).

### Uart8

```csharp
public const string Uart8
```

UART port on PE0 (TX) and PE1 (RX).

### Uart1

```csharp
public const string Uart1
```

UART port on PA9 (TX) and PA10 (RX).

### Uart2

```csharp
public const string Uart2
```

UART port on PD5 (TX) and PD6 (RX), PD3 (CTS) and PD4 (RTS).

### Uart4

```csharp
public const string Uart4
```

UART port on PD0 (TX) and PD1 (RX).

### Uart7

```csharp
public const string Uart7
```

UART port on PE8 (TX) and PE7 (RX).

### Uart8

```csharp
public const string Uart8
```

UART port on PE0 (TX) and PE1 (RX).

### Uart2

```csharp
public const string Uart2
```

UART port on PA2 (TX) and PA3 (RX), PA1 (CTS) and PA0 (RTS).

### Uart4

```csharp
public const string Uart4
```

UART port on PA0 (TX) and PA1 (RX).

### Uart1

```csharp
public const string Uart1
```

UART port on PA9 (TX) and PA10 (RX)

### Uart2

```csharp
public const string Uart2
```

UART port on PA2 (TX) and PA3 (RX), PA1 (CTS) and PA0 (RTS).

### Uart4

```csharp
public const string Uart4
```

UART port on PA0 (TX) and PA1 (RX).

### Uart2

```csharp
public const string Uart2
```

UART port on PD5 (TX) and PD6 (RX)

### MikroBus

```csharp
public const string MikroBus
```

UART port on PD5 (TX) and PD6 (RX)

### Uart4

```csharp
public const string Uart4
```

UART port on PH13 (TX) and PH14 (RX).

### Uart5

```csharp
public const string Uart5
```

UART port on PB13 (TX) and PB12 (RX).

### Uart6

```csharp
public const string Uart6
```

UART port on PC6 (TX) and PC7 (RX).

### Uart7

```csharp
public const string Uart7
```

UART port on PF7 (TX) and PF6 (RX), PF9 (CTS) and PF8 (RTS).

### Uart8

```csharp
public const string Uart8
```

UART port on PJ8 (TX) and PJ9 (RX).

### Uart1

```csharp
public const string Uart1
```

UART port on PA9 (TX) and PA10 (RX).

### Uart5

```csharp
public const string Uart5
```

UART port on PB13 (TX) and PB12 (RX), PC9 (CTS) and PC8 (RTS).

### Uart1

```csharp
public const string Uart1
```

UART port on PA9 (TX) and PA10 (RX).

### Uart2

```csharp
public const string Uart2
```

UART port on PA2 (TX) and PA3 (RX), PA1 (CTS) and PA0 (RTS).

### Uart3

```csharp
public const string Uart3
```

UART port on PB10 (TX) and PB11 (RX), PB1 (CTS) and PA6 (RTS).

### Uart4

```csharp
public const string Uart4
```

UART port on PA0 (TX) and PA1 (RX).

### Uart1

```csharp
public const string Uart1
```

UART port on PA9 (TX) and PA10 (RX).

### Uart2

```csharp
public const string Uart2
```

UART port on PD5 (TX) and PD6 (RX), PD3 (CTS) and PD4 (RTS).

### Uart3

```csharp
public const string Uart3
```

UART port on PD8 (TX) and PD9 (RX).

### Uart4

```csharp
public const string Uart4
```

UART port on PD0 (TX) and PD1 (RX), PB0 (CTS) and PA15 (RTS).

### Uart5

```csharp
public const string Uart5
```

UART port on PB13 (TX) and PB12 (RX), PC9 (CTS) and PC8 (RTS).

### Uart6

```csharp
public const string Uart6
```

UART port on PC6 (TX) and PC7 (RX).

### Uart7

```csharp
public const string Uart7
```

UART port on PE8 (TX) and PE7 (RX), PE10 (CTS) and PE9 (RTS).

### Uart8

```csharp
public const string Uart8
```

UART port on PE0 (TX) and PE1 (RX).

### Uart1

```csharp
public const string Uart1
```

UART port on PA9 (TX) and PA10 (RX).

### Uart2

```csharp
public const string Uart2
```

UART port on PD5 (TX) and PD6 (RX), PD3 (CTS) and PD4 (RTS).

### Uart3

```csharp
public const string Uart3
```

UART port on PC10 (TX) and PC11 (RX).

### Uart4

```csharp
public const string Uart4
```

UART port on PH13 (TX) and PH14 (RX), PB0 (CTS) and PA15 (RTS).

### Uart5

```csharp
public const string Uart5
```

UART port on PB13 (TX) and PB12 (RX), PC9 (CTS) and PC8 (RTS).

### Uart6

```csharp
public const string Uart6
```

UART port on PC6 (TX) and PC7 (RX).

### Uart7

```csharp
public const string Uart7
```

UART port on PF7 (TX) and PF6 (RX), PF9 (CTS) and PF8 (RTS).

### Uart8

```csharp
public const string Uart8
```

UART port on PJ8 (TX) and PJ9 (RX).

### Usart1

```csharp
public const string Usart1
```

UART port.

### Usart2

```csharp
public const string Usart2
```

UART port.

### Usart3

```csharp
public const string Usart3
```

UART port.

### Uart4

```csharp
public const string Uart4
```

UART port.

### Uart5

```csharp
public const string Uart5
```

UART port.

### Usart6

```csharp
public const string Usart6
```

UART port.

### Uart7

```csharp
public const string Uart7
```

UART port.

### Uart8

```csharp
public const string Uart8
```

UART port.

### Uart9

```csharp
public const string Uart9
```

UART port.

### Usart1

```csharp
public const string Usart1
```

UART port.

### Usart2

```csharp
public const string Usart2
```

UART port.

### Usart3

```csharp
public const string Usart3
```

UART port.

### Uart4

```csharp
public const string Uart4
```

UART port.
